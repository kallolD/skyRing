To generate the plots in the paper, run the `plots.ipynb` notebook. The data needed for this are included in the zenodo repo. 

By default, `plots.ipynb` assumes that the `posterior_files` directory (from zenodo) shares the same file location. Modify if needed.